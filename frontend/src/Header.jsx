//import './Header.css';

// React component that renders the header of the app.

const Header = () => {
  return (
    <header>
      <h1>BookStock Catalog</h1>
    </header>
  );
};

export default Header;
